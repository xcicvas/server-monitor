services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: server-monitor-agent
    restart: unless-stopped
    ports:
      - "7001:7001"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - AGENT_PORT=7001
      # 如需设置 JWT 密钥：
      # - JWT_SECRET=your_very_long_and_secure_secret_key_here
      # 如需限制 CORS：
      # - ALLOWED_ORIGINS=http://localhost:5173,https://your.domain.com
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:7001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
